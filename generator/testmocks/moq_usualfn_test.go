// Code generated by Moqueries - https://github.com/myshkin5/moqueries - DO NOT EDIT!

package testmocks_test

import (
	"math/bits"
	"sync/atomic"

	"github.com/myshkin5/moqueries/generator/testmocks"
	"github.com/myshkin5/moqueries/moq"
)

// mockUsualFn holds the state of a mock of the UsualFn type
type mockUsualFn struct {
	scene           *moq.Scene
	config          moq.MockConfig
	resultsByParams []mockUsualFn_resultsByParams
}

// mockUsualFn_mock isolates the mock interface of the UsualFn type
type mockUsualFn_mock struct {
	mock *mockUsualFn
}

// mockUsualFn_params holds the params of the UsualFn type
type mockUsualFn_params struct {
	sParam string
	bParam bool
}

// mockUsualFn_paramsKey holds the map key params of the UsualFn type
type mockUsualFn_paramsKey struct {
	sParam string
	bParam bool
}

// mockUsualFn_resultsByParams contains the results for a given set of parameters for the UsualFn type
type mockUsualFn_resultsByParams struct {
	anyCount  int
	anyParams uint64
	results   map[mockUsualFn_paramsKey]*mockUsualFn_results
}

// mockUsualFn_doFn defines the type of function needed when calling andDo for the UsualFn type
type mockUsualFn_doFn func(sParam string, bParam bool)

// mockUsualFn_doReturnFn defines the type of function needed when calling doReturnResults for the UsualFn type
type mockUsualFn_doReturnFn func(sParam string, bParam bool) (sResult string, err error)

// mockUsualFn_results holds the results of the UsualFn type
type mockUsualFn_results struct {
	params  mockUsualFn_params
	results []struct {
		values *struct {
			sResult string
			err     error
		}
		sequence   uint32
		doFn       mockUsualFn_doFn
		doReturnFn mockUsualFn_doReturnFn
	}
	index    uint32
	anyTimes bool
}

// mockUsualFn_fnRecorder routes recorded function calls to the mockUsualFn mock
type mockUsualFn_fnRecorder struct {
	params    mockUsualFn_params
	paramsKey mockUsualFn_paramsKey
	anyParams uint64
	sequence  bool
	results   *mockUsualFn_results
	mock      *mockUsualFn
}

// newMockUsualFn creates a new mock of the UsualFn type
func newMockUsualFn(scene *moq.Scene, config *moq.MockConfig) *mockUsualFn {
	if config == nil {
		config = &moq.MockConfig{}
	}
	m := &mockUsualFn{
		scene:  scene,
		config: *config,
	}
	scene.AddMock(m)
	return m
}

// mock returns the mock implementation of the UsualFn type
func (m *mockUsualFn) mock() testmocks.UsualFn {
	return func(sParam string, bParam bool) (sResult string, err error) {
		mock := &mockUsualFn_mock{mock: m}
		return mock.fn(sParam, bParam)
	}
}

func (m *mockUsualFn_mock) fn(sParam string, bParam bool) (sResult string, err error) {
	params := mockUsualFn_params{
		sParam: sParam,
		bParam: bParam,
	}
	var results *mockUsualFn_results
	for _, resultsByParams := range m.mock.resultsByParams {
		var sParamUsed string
		if resultsByParams.anyParams&(1<<0) == 0 {
			sParamUsed = sParam
		}
		var bParamUsed bool
		if resultsByParams.anyParams&(1<<1) == 0 {
			bParamUsed = bParam
		}
		paramsKey := mockUsualFn_paramsKey{
			sParam: sParamUsed,
			bParam: bParamUsed,
		}
		var ok bool
		results, ok = resultsByParams.results[paramsKey]
		if ok {
			break
		}
	}
	if results == nil {
		if m.mock.config.Expectation == moq.Strict {
			m.mock.scene.MoqT.Fatalf("Unexpected call with parameters %#v", params)
		}
		return
	}

	i := int(atomic.AddUint32(&results.index, 1)) - 1
	if i >= len(results.results) {
		if !results.anyTimes {
			if m.mock.config.Expectation == moq.Strict {
				m.mock.scene.MoqT.Fatalf("Too many calls to mock with parameters %#v", params)
			}
			return
		}
		i = len(results.results) - 1
	}

	result := results.results[i]
	if result.sequence != 0 {
		sequence := m.mock.scene.NextMockSequence()
		if (!results.anyTimes && result.sequence != sequence) || result.sequence > sequence {
			m.mock.scene.MoqT.Fatalf("Call sequence does not match %#v", params)
		}
	}

	if result.doFn != nil {
		result.doFn(sParam, bParam)
	}

	if result.values != nil {
		sResult = result.values.sResult
		err = result.values.err
	}
	if result.doReturnFn != nil {
		sResult, err = result.doReturnFn(sParam, bParam)
	}
	return
}

func (m *mockUsualFn) onCall(sParam string, bParam bool) *mockUsualFn_fnRecorder {
	return &mockUsualFn_fnRecorder{
		params: mockUsualFn_params{
			sParam: sParam,
			bParam: bParam,
		},
		paramsKey: mockUsualFn_paramsKey{
			sParam: sParam,
			bParam: bParam,
		},
		sequence: m.config.Sequence == moq.SeqDefaultOn,
		mock:     m,
	}
}

func (r *mockUsualFn_fnRecorder) anySParam() *mockUsualFn_fnRecorder {
	if r.results != nil {
		r.mock.scene.MoqT.Fatalf("Any functions must be called before returnResults or doReturnResults calls, parameters: %#v", r.params)
		return nil
	}
	r.anyParams |= 1 << 0
	return r
}

func (r *mockUsualFn_fnRecorder) anyBParam() *mockUsualFn_fnRecorder {
	if r.results != nil {
		r.mock.scene.MoqT.Fatalf("Any functions must be called before returnResults or doReturnResults calls, parameters: %#v", r.params)
		return nil
	}
	r.anyParams |= 1 << 1
	return r
}

func (r *mockUsualFn_fnRecorder) seq() *mockUsualFn_fnRecorder {
	if r.results != nil {
		r.mock.scene.MoqT.Fatalf("seq must be called before returnResults or doReturnResults calls, parameters: %#v", r.params)
		return nil
	}
	r.sequence = true
	return r
}

func (r *mockUsualFn_fnRecorder) noSeq() *mockUsualFn_fnRecorder {
	if r.results != nil {
		r.mock.scene.MoqT.Fatalf("noSeq must be called before returnResults or doReturnResults calls, parameters: %#v", r.params)
		return nil
	}
	r.sequence = false
	return r
}

func (r *mockUsualFn_fnRecorder) returnResults(sResult string, err error) *mockUsualFn_fnRecorder {
	r.findResults()

	var sequence uint32
	if r.sequence {
		sequence = r.mock.scene.NextRecorderSequence()
	}

	r.results.results = append(r.results.results, struct {
		values *struct {
			sResult string
			err     error
		}
		sequence   uint32
		doFn       mockUsualFn_doFn
		doReturnFn mockUsualFn_doReturnFn
	}{
		values: &struct {
			sResult string
			err     error
		}{
			sResult: sResult,
			err:     err,
		},
		sequence: sequence,
	})
	return r
}

func (r *mockUsualFn_fnRecorder) andDo(fn mockUsualFn_doFn) *mockUsualFn_fnRecorder {
	if r.results == nil {
		r.mock.scene.MoqT.Fatalf("returnResults must be called before calling andDo")
		return nil
	}
	last := &r.results.results[len(r.results.results)-1]
	last.doFn = fn
	return r
}

func (r *mockUsualFn_fnRecorder) doReturnResults(fn mockUsualFn_doReturnFn) *mockUsualFn_fnRecorder {
	r.findResults()

	var sequence uint32
	if r.sequence {
		sequence = r.mock.scene.NextRecorderSequence()
	}

	r.results.results = append(r.results.results, struct {
		values *struct {
			sResult string
			err     error
		}
		sequence   uint32
		doFn       mockUsualFn_doFn
		doReturnFn mockUsualFn_doReturnFn
	}{sequence: sequence, doReturnFn: fn})
	return r
}

func (r *mockUsualFn_fnRecorder) findResults() {
	if r.results == nil {
		anyCount := bits.OnesCount64(r.anyParams)
		insertAt := -1
		var results *mockUsualFn_resultsByParams
		for n, res := range r.mock.resultsByParams {
			if res.anyParams == r.anyParams {
				results = &res
				break
			}
			if res.anyCount > anyCount {
				insertAt = n
			}
		}
		if results == nil {
			results = &mockUsualFn_resultsByParams{
				anyCount:  anyCount,
				anyParams: r.anyParams,
				results:   map[mockUsualFn_paramsKey]*mockUsualFn_results{},
			}
			r.mock.resultsByParams = append(r.mock.resultsByParams, *results)
			if insertAt != -1 && insertAt+1 < len(r.mock.resultsByParams) {
				copy(r.mock.resultsByParams[insertAt+1:], r.mock.resultsByParams[insertAt:0])
				r.mock.resultsByParams[insertAt] = *results
			}
		}

		var sParamUsed string
		if r.anyParams&(1<<0) == 0 {
			sParamUsed = r.paramsKey.sParam
		}
		var bParamUsed bool
		if r.anyParams&(1<<1) == 0 {
			bParamUsed = r.paramsKey.bParam
		}
		paramsKey := mockUsualFn_paramsKey{
			sParam: sParamUsed,
			bParam: bParamUsed,
		}

		var ok bool
		r.results, ok = results.results[paramsKey]
		if !ok {
			r.results = &mockUsualFn_results{
				params:   r.params,
				results:  nil,
				index:    0,
				anyTimes: false,
			}
			results.results[paramsKey] = r.results
		}
	}
}

func (r *mockUsualFn_fnRecorder) times(count int) *mockUsualFn_fnRecorder {
	if r.results == nil {
		r.mock.scene.MoqT.Fatalf("returnResults or doReturnResults must be called before calling times")
		return nil
	}
	last := r.results.results[len(r.results.results)-1]
	for n := 0; n < count-1; n++ {
		if last.sequence != 0 {
			last = struct {
				values *struct {
					sResult string
					err     error
				}
				sequence   uint32
				doFn       mockUsualFn_doFn
				doReturnFn mockUsualFn_doReturnFn
			}{
				values: &struct {
					sResult string
					err     error
				}{
					sResult: last.values.sResult,
					err:     last.values.err,
				},
				sequence: r.mock.scene.NextRecorderSequence(),
			}
		}
		r.results.results = append(r.results.results, last)
	}
	return r
}

func (r *mockUsualFn_fnRecorder) anyTimes() {
	if r.results == nil {
		r.mock.scene.MoqT.Fatalf("returnResults or doReturnResults must be called before calling anyTimes")
		return
	}
	r.results.anyTimes = true
}

// Reset resets the state of the mock
func (m *mockUsualFn) Reset() { m.resultsByParams = nil }

// AssertExpectationsMet asserts that all expectations have been met
func (m *mockUsualFn) AssertExpectationsMet() {
	for _, res := range m.resultsByParams {
		for _, results := range res.results {
			missing := len(results.results) - int(atomic.LoadUint32(&results.index))
			if missing == 1 && results.anyTimes == true {
				continue
			}
			if missing > 0 {
				m.scene.MoqT.Errorf("Expected %d additional call(s) with parameters %#v", missing, results.params)
			}
		}
	}
}
