// Code generated by Moqueries - https://github.com/myshkin5/moqueries - DO NOT EDIT!

package generator_test

import (
	"math/bits"
	"sync/atomic"

	"github.com/myshkin5/moqueries/generator"
	"github.com/myshkin5/moqueries/hash"
	"github.com/myshkin5/moqueries/moq"
)

// moqFindPackageFn holds the state of a moq of the FindPackageFn type
type moqFindPackageFn struct {
	scene  *moq.Scene
	config moq.Config
	moq    *moqFindPackageFn_mock

	resultsByParams []moqFindPackageFn_resultsByParams

	runtime struct {
		parameterIndexing struct {
			pattern moq.ParamIndexing
		}
	}
}

// moqFindPackageFn_mock isolates the mock interface of the FindPackageFn type
type moqFindPackageFn_mock struct {
	moq *moqFindPackageFn
}

// moqFindPackageFn_params holds the params of the FindPackageFn type
type moqFindPackageFn_params struct{ pattern string }

// moqFindPackageFn_paramsKey holds the map key params of the FindPackageFn type
type moqFindPackageFn_paramsKey struct {
	params struct{ pattern string }
	hashes struct{ pattern hash.Hash }
}

// moqFindPackageFn_resultsByParams contains the results for a given set of parameters for the FindPackageFn type
type moqFindPackageFn_resultsByParams struct {
	anyCount  int
	anyParams uint64
	results   map[moqFindPackageFn_paramsKey]*moqFindPackageFn_results
}

// moqFindPackageFn_doFn defines the type of function needed when calling andDo for the FindPackageFn type
type moqFindPackageFn_doFn func(pattern string)

// moqFindPackageFn_doReturnFn defines the type of function needed when calling doReturnResults for the FindPackageFn type
type moqFindPackageFn_doReturnFn func(pattern string) (pkg string, err error)

// moqFindPackageFn_results holds the results of the FindPackageFn type
type moqFindPackageFn_results struct {
	params  moqFindPackageFn_params
	results []struct {
		values *struct {
			pkg string
			err error
		}
		sequence   uint32
		doFn       moqFindPackageFn_doFn
		doReturnFn moqFindPackageFn_doReturnFn
	}
	index  uint32
	repeat *moq.RepeatVal
}

// moqFindPackageFn_fnRecorder routes recorded function calls to the moqFindPackageFn moq
type moqFindPackageFn_fnRecorder struct {
	params    moqFindPackageFn_params
	anyParams uint64
	sequence  bool
	results   *moqFindPackageFn_results
	moq       *moqFindPackageFn
}

// moqFindPackageFn_anyParams isolates the any params functions of the FindPackageFn type
type moqFindPackageFn_anyParams struct {
	recorder *moqFindPackageFn_fnRecorder
}

// newMoqFindPackageFn creates a new moq of the FindPackageFn type
func newMoqFindPackageFn(scene *moq.Scene, config *moq.Config) *moqFindPackageFn {
	if config == nil {
		config = &moq.Config{}
	}
	m := &moqFindPackageFn{
		scene:  scene,
		config: *config,
		moq:    &moqFindPackageFn_mock{},

		runtime: struct {
			parameterIndexing struct {
				pattern moq.ParamIndexing
			}
		}{parameterIndexing: struct {
			pattern moq.ParamIndexing
		}{
			pattern: moq.ParamIndexByValue,
		}},
	}
	m.moq.moq = m

	scene.AddMoq(m)
	return m
}

// mock returns the moq implementation of the FindPackageFn type
func (m *moqFindPackageFn) mock() generator.FindPackageFn {
	return func(pattern string) (_ string, _ error) {
		moq := &moqFindPackageFn_mock{moq: m}
		return moq.fn(pattern)
	}
}

func (m *moqFindPackageFn_mock) fn(pattern string) (pkg string, err error) {
	params := moqFindPackageFn_params{
		pattern: pattern,
	}
	var results *moqFindPackageFn_results
	for _, resultsByParams := range m.moq.resultsByParams {
		paramsKey := m.moq.paramsKey(params, resultsByParams.anyParams)
		var ok bool
		results, ok = resultsByParams.results[paramsKey]
		if ok {
			break
		}
	}
	if results == nil {
		if m.moq.config.Expectation == moq.Strict {
			m.moq.scene.T.Fatalf("Unexpected call with parameters %#v", params)
		}
		return
	}

	i := int(atomic.AddUint32(&results.index, 1)) - 1
	if i >= results.repeat.ResultCount {
		if !results.repeat.AnyTimes {
			if m.moq.config.Expectation == moq.Strict {
				m.moq.scene.T.Fatalf("Too many calls to mock with parameters %#v", params)
			}
			return
		}
		i = results.repeat.ResultCount - 1
	}

	result := results.results[i]
	if result.sequence != 0 {
		sequence := m.moq.scene.NextMockSequence()
		if (!results.repeat.AnyTimes && result.sequence != sequence) || result.sequence > sequence {
			m.moq.scene.T.Fatalf("Call sequence does not match %#v", params)
		}
	}

	if result.doFn != nil {
		result.doFn(pattern)
	}

	if result.values != nil {
		pkg = result.values.pkg
		err = result.values.err
	}
	if result.doReturnFn != nil {
		pkg, err = result.doReturnFn(pattern)
	}
	return
}

func (m *moqFindPackageFn) onCall(pattern string) *moqFindPackageFn_fnRecorder {
	return &moqFindPackageFn_fnRecorder{
		params: moqFindPackageFn_params{
			pattern: pattern,
		},
		sequence: m.config.Sequence == moq.SeqDefaultOn,
		moq:      m,
	}
}

func (r *moqFindPackageFn_fnRecorder) any() *moqFindPackageFn_anyParams {
	if r.results != nil {
		r.moq.scene.T.Fatalf("Any functions must be called before returnResults or doReturnResults calls, parameters: %#v", r.params)
		return nil
	}
	return &moqFindPackageFn_anyParams{recorder: r}
}

func (a *moqFindPackageFn_anyParams) pattern() *moqFindPackageFn_fnRecorder {
	a.recorder.anyParams |= 1 << 0
	return a.recorder
}

func (r *moqFindPackageFn_fnRecorder) seq() *moqFindPackageFn_fnRecorder {
	if r.results != nil {
		r.moq.scene.T.Fatalf("seq must be called before returnResults or doReturnResults calls, parameters: %#v", r.params)
		return nil
	}
	r.sequence = true
	return r
}

func (r *moqFindPackageFn_fnRecorder) noSeq() *moqFindPackageFn_fnRecorder {
	if r.results != nil {
		r.moq.scene.T.Fatalf("noSeq must be called before returnResults or doReturnResults calls, parameters: %#v", r.params)
		return nil
	}
	r.sequence = false
	return r
}

func (r *moqFindPackageFn_fnRecorder) returnResults(pkg string, err error) *moqFindPackageFn_fnRecorder {
	r.findResults()

	var sequence uint32
	if r.sequence {
		sequence = r.moq.scene.NextRecorderSequence()
	}

	r.results.results = append(r.results.results, struct {
		values *struct {
			pkg string
			err error
		}
		sequence   uint32
		doFn       moqFindPackageFn_doFn
		doReturnFn moqFindPackageFn_doReturnFn
	}{
		values: &struct {
			pkg string
			err error
		}{
			pkg: pkg,
			err: err,
		},
		sequence: sequence,
	})
	return r
}

func (r *moqFindPackageFn_fnRecorder) andDo(fn moqFindPackageFn_doFn) *moqFindPackageFn_fnRecorder {
	if r.results == nil {
		r.moq.scene.T.Fatalf("returnResults must be called before calling andDo")
		return nil
	}
	last := &r.results.results[len(r.results.results)-1]
	last.doFn = fn
	return r
}

func (r *moqFindPackageFn_fnRecorder) doReturnResults(fn moqFindPackageFn_doReturnFn) *moqFindPackageFn_fnRecorder {
	r.findResults()

	var sequence uint32
	if r.sequence {
		sequence = r.moq.scene.NextRecorderSequence()
	}

	r.results.results = append(r.results.results, struct {
		values *struct {
			pkg string
			err error
		}
		sequence   uint32
		doFn       moqFindPackageFn_doFn
		doReturnFn moqFindPackageFn_doReturnFn
	}{sequence: sequence, doReturnFn: fn})
	return r
}

func (r *moqFindPackageFn_fnRecorder) findResults() {
	if r.results != nil {
		r.results.repeat.Increment(r.moq.scene.T)
		return
	}

	anyCount := bits.OnesCount64(r.anyParams)
	insertAt := -1
	var results *moqFindPackageFn_resultsByParams
	for n, res := range r.moq.resultsByParams {
		if res.anyParams == r.anyParams {
			results = &res
			break
		}
		if res.anyCount > anyCount {
			insertAt = n
		}
	}
	if results == nil {
		results = &moqFindPackageFn_resultsByParams{
			anyCount:  anyCount,
			anyParams: r.anyParams,
			results:   map[moqFindPackageFn_paramsKey]*moqFindPackageFn_results{},
		}
		r.moq.resultsByParams = append(r.moq.resultsByParams, *results)
		if insertAt != -1 && insertAt+1 < len(r.moq.resultsByParams) {
			copy(r.moq.resultsByParams[insertAt+1:], r.moq.resultsByParams[insertAt:0])
			r.moq.resultsByParams[insertAt] = *results
		}
	}

	paramsKey := r.moq.paramsKey(r.params, r.anyParams)

	var ok bool
	r.results, ok = results.results[paramsKey]
	if !ok {
		r.results = &moqFindPackageFn_results{
			params:  r.params,
			results: nil,
			index:   0,
			repeat:  &moq.RepeatVal{},
		}
		results.results[paramsKey] = r.results
	}

	r.results.repeat.Increment(r.moq.scene.T)
}

func (r *moqFindPackageFn_fnRecorder) repeat(repeaters ...moq.Repeater) *moqFindPackageFn_fnRecorder {
	if r.results == nil {
		r.moq.scene.T.Fatalf("returnResults or doReturnResults must be called before calling repeat")
		return nil
	}
	r.results.repeat.Repeat(r.moq.scene.T, repeaters)
	last := r.results.results[len(r.results.results)-1]
	for n := 0; n < r.results.repeat.ResultCount-1; n++ {
		if r.sequence {
			last = struct {
				values *struct {
					pkg string
					err error
				}
				sequence   uint32
				doFn       moqFindPackageFn_doFn
				doReturnFn moqFindPackageFn_doReturnFn
			}{
				values: &struct {
					pkg string
					err error
				}{
					pkg: last.values.pkg,
					err: last.values.err,
				},
				sequence: r.moq.scene.NextRecorderSequence(),
			}
		}
		r.results.results = append(r.results.results, last)
	}
	return r
}

func (m *moqFindPackageFn) paramsKey(params moqFindPackageFn_params, anyParams uint64) moqFindPackageFn_paramsKey {
	var patternUsed string
	var patternUsedHash hash.Hash
	if anyParams&(1<<0) == 0 {
		if m.runtime.parameterIndexing.pattern == moq.ParamIndexByValue {
			patternUsed = params.pattern
		} else {
			patternUsedHash = hash.DeepHash(params.pattern)
		}
	}
	return moqFindPackageFn_paramsKey{
		params: struct{ pattern string }{
			pattern: patternUsed,
		},
		hashes: struct{ pattern hash.Hash }{
			pattern: patternUsedHash,
		},
	}
}

// Reset resets the state of the moq
func (m *moqFindPackageFn) Reset() { m.resultsByParams = nil }

// AssertExpectationsMet asserts that all expectations have been met
func (m *moqFindPackageFn) AssertExpectationsMet() {
	for _, res := range m.resultsByParams {
		for _, results := range res.results {
			missing := results.repeat.MinTimes - int(atomic.LoadUint32(&results.index))
			if missing > 0 {
				m.scene.T.Errorf("Expected %d additional call(s) with parameters %#v", missing, results.params)
			}
		}
	}
}
